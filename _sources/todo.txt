
.. _reference:


Future
========

Next
------------
* transform into wikilytics package
	* write scripts 
		* for running all three workflow steps
		* automatic creating of a config file.
	* create an example new stand-alone cohort, e.g. for new editors (already done). Or some random with 
* move 'filterbot' settings to the cohort level instead of global setting
* visualizations
	* move wikipride plots into viz (OR NOT?)
	* create module for the all line plots (that depend on a specific cohort class). Should they be moved into the cohort class itself?
* cohort definitions
	* count the number of editors in the `simple.namespaces` cohort.
	* article creation cohorts

